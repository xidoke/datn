export interface State {
  id: string;
  name: string;
  color: string;
  // projectId: string;
  isDefault?: boolean;
  description?: string;
  group: string;
  createdAt?: string;
  updatedAt?: string;
}
