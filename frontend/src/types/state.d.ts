export interface State {
  id: string;
  name: string;
  color: string;
  // projectId: string;p`
  isDefault?: boolean;
  description?: string;
  group: string;
  createdAt?: string;
  updatedAt?: string;
}
